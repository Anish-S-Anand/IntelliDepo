import StreamLayout from "@/components/stream/layout/StreamLayout";

export default function StreamRouteLayout({ children }: { children: React.ReactNode }) {
  return <StreamLayout>{children}</StreamLayout>;
}
