import DepotLayout from "@/components/depot/layout/DepotLayout";

export default function DepotRouteLayout({ children }: { children: React.ReactNode }) {
  return <DepotLayout>{children}</DepotLayout>;
}
